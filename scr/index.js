// Quando o HTML terminar de carregar ela vai executa essa função
document.addEventListener("DOMContentLoaded", () => {
    console.log("Landing page carregada!");// Mostra essa mensagem no console do navegador (serve só pra avisar que a página carregou)

});
